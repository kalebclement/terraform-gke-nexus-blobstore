# bound to a GCP SA via Workload Identity
# annotation value comes from terraform's gcs_service_account_email output
apiVersion: v1
kind: ServiceAccount
metadata:
  name: nexus
  namespace: nexus
  annotations:
    iam.gke.io/gcp-service-account: REPLACE_WITH_GSA_EMAIL
